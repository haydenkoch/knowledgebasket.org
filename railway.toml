[build]
builder = "RAILPACK"
buildCommand = "pnpm build"

[deploy]
preDeployCommand = "pnpm db:migrate"
startCommand = "pnpm start:app"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
